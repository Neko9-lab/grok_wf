**Language / 语言:** [English](#english) · [中文](#中文)

---

## English

### gwf-cli 0.1.0

First public release: `gwf init` / `update` / `doctor`, `.gwf/` + `.grok/` adapters, scope checks. Prefer **0.1.6**.

```bash
npm install -g gwf-cli@0.1.0 --registry https://registry.npmjs.org/
```

---

## 中文

### gwf-cli 0.1.0

首个公开发布：`gwf init` / `update` / `doctor`、`.gwf/` + `.grok/` 适配、范围检查。日常请用 **0.1.6**。

```bash
npm install -g gwf-cli@0.1.0 --registry https://registry.npmjs.org/
```
